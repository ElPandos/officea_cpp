//#include "General.h"

/*
class CThread
{
public:

	CThread() // Constructor
	{

	}; 

	~CThread() {} // deConstructor
	

	SDL_Thread* CreateThread(void* pThreadedFunction, string sThreadName);
	void WaitThread(SDL_Thread* pThread);
	void LockThread(void* pThreadedFunction);

private:

	SDL_mutex* m_pMut;
	SDL_Thread* m_pThread;

};

//SDL_Thread* pMain = SDL_CreateThread( InitMain, "InitMain", (void *)NULL);
//SDL_Thread* pLog = SDL_CreateThread( InitLog, "InitLog", (void *)NULL);
//SDL_Thread* pTest = SDL_CreateThread( TestThread, "TestThread", (void *)NULL);

//SDL_Delay(10000);
*/